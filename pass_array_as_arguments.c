#include<stdio.h>
int avg(int[],int);
int size,i,sum=0;

int main(){
int average;

int marks[5]={10,30,20,50,40};
}

int avg(marks,a){
for(i=0;i<a;i++){
    sum=sum+marks[i];
}
size=sizeof(marks)/sizeof(marks[]);
}
